#!/bin/bash
DEB_DIR="/opt/tenebra-packages"
if [ -d "$DEB_DIR" ] && ls "$DEB_DIR"/*.deb >/dev/null 2>&1; then
    dpkg -i "$DEB_DIR"/*.deb 2>/dev/null || true
    apt-get install -fy 2>/dev/null || true
fi

# SDDM is enabled by runsvdir via /etc/sv (see 0040-runit hook)
