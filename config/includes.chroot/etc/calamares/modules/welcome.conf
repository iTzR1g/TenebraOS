# welcome — hardware requirements only; never block on internet
---
requirements:
    requiredStorage: 15
    requiredRam: 2.0
    check:
        - storage
        - ram
    required:
        - storage
        - ram
