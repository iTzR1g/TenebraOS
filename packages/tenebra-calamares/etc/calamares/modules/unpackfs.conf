# unpackfs — copy the live squashfs onto the target
# (live-boot mounts the ISO medium at /run/live/medium)
---
unpack:
    - source: "/run/live/medium/live/filesystem.squashfs"
      sourcefs: "squashfs"
      destination: ""
