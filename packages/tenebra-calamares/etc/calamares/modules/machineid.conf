# machineid — Devuan (runit) target: No systemd-machine-id-setup exists,
# so disable the systemd style and only create the D-Bus machine-id.
---
systemd: false
dbus: true
symlink: false
entropy-copy: false